# Normalize line endings: text files stored as LF in the repo,
# checked out per platform. Repo-controlled so behavior doesn't
# depend on each machine's core.autocrlf setting.
* text=auto

# Windows batch files must stay CRLF
*.bat text eol=crlf

# Binary files
*.pdf binary
*.png binary
*.ico binary
*.zip binary
